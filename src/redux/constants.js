// Збережемо можливі значення фільтра як об'єкта,
// щоб повторно використовувати їх у різних місцях програми
export const statusFilters = Object.freeze({
  all: 'all',
  active: 'active',
  completed: 'completed',
});
